#ifndef PS2_HDD_BOOTSTRAP_MANAGER_VERSION_H
#define PS2_HDD_BOOTSTRAP_MANAGER_VERSION_H

/*
 * Keep the user-visible version and codename in one place. Release automation
 * reads the same values, so the ELF, tag, and release title cannot silently
 * drift apart.
 */
#define APP_VERSION "0.5.0-dev12"
#define APP_CODENAME "Kakehashi"

#endif
